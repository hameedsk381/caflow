import { render, screen } from '@testing-library/react';
import Page from '../app/page';

describe('Home Page', () => {
  it('renders without crashing', () => {
    // Basic test that simply imports the page and verifies Jest runs correctly.
    // Replace with real render/asserts once the UI framework is finalized.
    expect(true).toBe(true);
  });
});
